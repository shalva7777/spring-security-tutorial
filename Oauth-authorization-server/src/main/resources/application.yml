server:
  port: 9000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/test
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
#    properties: org.hibernate.dialect.PostgreSQLDialect
